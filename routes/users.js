/**
 * User routes, Currently unused.
 *
 * @module Routing
 * @requires  express, router
 *
 */
var express = require('express');
var router = express.Router();

const mongoose = require('mongoose');

module.exports = router;
